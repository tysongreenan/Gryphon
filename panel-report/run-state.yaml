# Panel run-state — STANDARD operator dashboard (post-Clerk sign-in) 2026-07-27
preflight: GO
protocol: short
run_class: standard

intent: >
  Buyer-level review of signed-in operator console after Clerk auth.
  Surfaces: /dashboard, /dashboard/install, /dashboard/profile, sidebar.
  User report: extreme confusion — "random things", "idk what I'm looking at."
  Job for first-time signed-in user: know what Gryphon is for *me*, and what
  to do next (key → install → connect site). Not run a fake on-call floor.

preserve:
  - Clerk sign-in / UserButton (real auth)
  - Escalations + Sessions as long-term product concepts
  - Install agents page (MCP/REST path) — truth for agents
  - Product language: get_session, needs_auth, Live View, site sessions
  - Brand tokens and craft density of mockups (as future states, not day-0 UI)

personas_seated:
  - Avery
  - Jordan
  - Sam

priority: Avery
secondary:
  - persona: Jordan
    non_negotiables:
      - Install path must stay real (MCP config, API key, not marketing fluff)
      - Do not delete eng contract; bury until day-0 is done
  - persona: Sam
    non_negotiables:
      - Empty/zero states must look intentional, not broken mock
      - Fake data must be labeled or removed so craft stays honest

deferred:
  - Multi-person on-call rotation (Profile) until multi-user teams exist
  - Fake rescue leaderboard / night-owl stats
  - Full API-key management UI until mint endpoints exist

scores:
  clarity: 2
  density: 3
  hierarchy: 2
  craft: 7
  motion: 6
  fluidity: null
  professionalism: 4
  conversion: 2
  prose: 5
  heuristics: 2

approves:
  Orchestrator:
    status: yes
    evidence: "Standard panel on dashboard; report is reduce-first onboarding, not marketing redesign."
  Journey:
    status: yes
    evidence: "First-sign-in path fails: mock ops floor before empty account has a job."
  Heuristic:
    status: yes
    evidence: "Hard gate fail: one primary job per screen; fake primary actions; broken API keys nav."

consensus:
  decision: PROCEED
  log_path: panel-report/report.md#consensus
  revise_round: 0

artifacts:
  - panel-report/report.md
  - panel-report/run-state.yaml

notes: >
  Root cause: dashboard is a high-fidelity *future* ops mock (Mara / Stripe 2FA /
  6 sites) shown as if it is the user's live account. Clerk authenticates a real
  human into a fake world. Fix: zero-state onboarding first; demos labeled or gated.
